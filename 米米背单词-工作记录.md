# 米米背单词（007）· 完整工作记录

> 记录日期：2026-09-09
> 项目路径：`/Users/H.Sheng/WorkBuddy/2026-09-09-09-42-17/mimi-vocab/007_米米背单词`
> 记录性质：上下文 + 技术知识 + 产出清单 + 验证数据 + 决策依据

---

## 一、项目概览

**是什么**：给米米做的看图拼写背单词工具，单文件 HTML 应用。

**核心学习闭环**：
```
只看插画（不给单词）→ 心里想词 → 点图片 → 按字母数出现下划线
→ 输入拼写 → 自动判定 → 拼对了才显示英文释义与例句
```

**设计原则**：
- 纯英文环境 —— 释义、例句一律英文，界面不出现中文
- 单文件交付 —— 插画 base64 内嵌，离线可用
- 单一数据源 —— 一切从 `vocab-builder/` 生成，成品可随时重建

**当前规模**：1 个单元（Text 1），15 个单词，15 张插画。

---

## 二、今日工作全景（时间线）

| 时间 | 事项 | 结果 |
|---|---|---|
| 09:42 | 接收 `007_米米背单词.zip`（22.5 MB），解压还原项目 | 成功，需处理 zip 中文名编码 |
| 09:43 | 核对资产完整性 | 15 词 / 15 原图 / 15 缩略图，全部完整 |
| 09:45–09:54 | 搭建 Python 隔离环境，安装 Pillow 12.3.0 | 耗时约 7 分钟（网络慢） |
| 09:54 | 首次运行 `build.py` | 输出 `images ok`；重建产物与沙箱产物**字节级一致** |
| 09:55 | 整理完整工作手册 `工作内容.md` | 12 章节 |
| 09:59 | 首次发布在线应用 | 返回 `*.app.workbuddy.link` |
| 10:57 | 用户反馈链接打不开 | 实测 404「链接已失效」 |
| 10:59 | 重新发布 | 返回新地址 `*.e2b.sh3.sandbox.cloudstudio.club`，当时 200 |
| 11:11 | 用户反馈仍打不开 | 两个链接分别 403 / 502，**该渠道判定不可用** |
| 11:15 | 排查环境：GitHub 不可达；Gitee/Netlify/Vercel/Surge 可达 | 形成决策依据 |
| 11:20 | 用户选定方案：腾讯云 COS + 便携版兜底 | — |
| 11:25 | 生成便携版单文件 | 616 KB，零外部依赖 |
| 11:30 | 编写 COS 部署脚本（遇 pip 沙箱限制，改纯标准库实现） | 签名链路验证通过 |
| 12:40 | 重建纯净便携版（发现预览服务注入污染） | 四份成品 MD5 完全一致 |

---

## 三、交付成果清单

### 3.1 成品文件（4 份，内容完全相同）

| 文件 | 用途 | MD5 |
|---|---|---|
| `背单词-学习卡片.html` | 主成品，双击即用 | `4fc7287c...d86496` |
| `index.html` | 项目根副本 | 同上 |
| `site/index.html` | 发布专用目录（仅此一个文件） | 同上 |
| `米米背单词-便携版.html` | **交付给使用者的版本** | 同上 |

四份 MD5 完全一致：`4fc7287c003a7badfdba1672bbd86496`，各 630,872 字节。

### 3.2 源数据与工具

| 文件 | 大小 | 说明 |
|---|---|---|
| `vocab-builder/units/Text1.json` | 8 KB | 词表数据（唯一数据源） |
| `vocab-builder/template.html` | 32 KB | HTML 模板，含 `/*__UNITDATA__*/` 占位符 |
| `vocab-builder/build.py` | 4 KB | 构建脚本，路径自解析 |
| `vocab-builder/deploy_cos.py` | 8 KB | 腾讯云 COS 部署脚本（纯标准库） |
| `vocab-builder/工作内容.md` | 12 KB | 持续维护的工作手册 |
| `vocab-builder/images/Text1/` | 21 MB | 15 张 PNG 原图 + 15 张 small JPG |

### 3.3 便携版自包含性验证（实测）

```
字节数        : 630,872
内嵌图片      : 15 张
外部 JS/CSS   : 0 个
注入残留      : 0 处
唯一外部 URL  : dict.youdao.com（在线真人发音，可选）
```

**结论**：断网可用。在线发音取不到时自动回退本地语音，不影响任何功能。

---

## 四、技术架构

### 4.1 目录结构

```
007_米米背单词/
├── 米米背单词-便携版.html      ← 交付版（发微信/拷手机）
├── 背单词-学习卡片.html        ← 主成品
├── index.html                 ← 根副本
├── site/index.html            ← 发布目录（620 KB，不含原图）
└── vocab-builder/             ← 唯一数据源
    ├── units/Text1.json
    ├── images/Text1/*.png     (15 张原图，21 MB)
    │   └── small/*.jpg        (15 张，构建时自动生成)
    ├── template.html
    ├── build.py
    ├── deploy_cos.py
    └── 工作内容.md
```

### 4.2 构建流程

```bash
cd /Users/H.Sheng/WorkBuddy/2026-09-09-09-42-17/mimi-vocab/007_米米背单词
/Users/H.Sheng/.workbuddy/binaries/python/envs/default/bin/python vocab-builder/build.py
```

必须使用上述 venv 路径 —— 本机其他 Python 均未安装 Pillow。

构建脚本行为：
1. 按自然序读取 `units/TextN.json`（保证 Text2 < Text10）
2. 按 `NN_slug.png` 匹配图片 → 转 RGB → 缩放 720×480 → JPEG q70
3. 压缩图写入 `images/<Unit>/small/`
4. 图片转 base64 内联，替换模板占位符
5. 同时输出三份：成品 HTML、根 `index.html`、`site/index.html`

### 4.3 数据规范

```json
{
  "id": "Text1",
  "title": "Text 1",
  "note": "来源说明",
  "words": [{
    "w": "accountable",      // 单词（小写，可含连字符）
    "pos": "adjective",      // 词性
    "ph": "/əˈkaʊntəbl/",    // 国际音标
    "def": "...",            // 英文释义（禁止中文）
    "note": "...",           // 搭配/易混词/构词提示
    "ex": "...",             // 英文例句
    "prompt": "..."          // 画图描述，便于重画
  }]
}
```

**约束**：
- `id` 必须与图片目录名一致（`Text1` ↔ `images/Text1/`）
- 图片命名 `NN_slug.png`，slug = 单词小写且 `-`/空格/`/` 替换为 `_`
- 例：`self-esteem` → `14_self_esteem.png`

### 4.4 当前词表（Text 1，15 词）

| # | 单词 | 词性 | 音标 |
|---|---|---|---|
| 1 | accountable | adjective | /əˈkaʊntəbl/ |
| 2 | advise | verb | /ədˈvaɪz/ |
| 3 | consider | verb | /kənˈsɪdə(r)/ |
| 4 | costume | noun | /ˈkɒstjuːm/ |
| 5 | desperately | adverb | /ˈdespərətli/ |
| 6 | hesitate | verb | /ˈhezɪteɪt/ |
| 7 | humiliate | verb | /hjuːˈmɪlieɪt/ |
| 8 | individual | noun | /ˌɪndɪˈvɪdʒuəl/ |
| 9 | inspiration | noun | /ˌɪnspəˈreɪʃn/ |
| 10 | interrupt | verb | /ˌɪntəˈrʌpt/ |
| 11 | juggle | verb | /ˈdʒʌɡl/ |
| 12 | resentful | adjective | /rɪˈzentfl/ |
| 13 | routine | noun | /ruːˈtiːn/ |
| 14 | self-esteem | noun | /ˌself ɪˈstiːm/ |
| 15 | uncomfortably | adverb | /ʌnˈkʌmftəbli/ |

---

## 五、产品功能

**1. Cards —— 主学习闭环**
- 仅显示插画；点图后缩小并出现下划线格子（数量 = 字母数；连字符单独显示且不计入输入）
- 边打边填格，输满自动判定；回车可提前判定
- 拼对 → 绿色 ✓ → 0.7 秒后进入结果页并自动朗读
- 拼错 → 红色抖动 → 显示正确拼写 → 2.2 秒后清空重拼
- `Reveal answer` 直接看答案（标红）
- 结果页：词性、单词、音标、英文释义、用法提示、例句，单词与例句各有独立朗读按钮
- 导航：Prev / Shuffle / Next；全部掌握后显示完成页

**2. Quiz —— 反向测验**
- 给英文释义四选一，**无图**
- 实时计分；答错停留更久（2.8s vs 1.5s）并朗读
- 结束按正确率反馈（≥90 🏆 / ≥70 👏 / 其余 💪），列出全部错词及释义

**3. List —— 词表**
- 一屏看完全部单词：缩略图 + 单词 + 音标 + 释义，点任意一行朗读

**4. 发音设置**
- 自动优选女声（按 natural/neural/online/samantha/karen 等关键词打分）
- 可调 voice / rate（默认 0.88）/ pitch（默认 1.08）
- 音源三档：Auto / Device only / Online human voice（有道 TTS，需联网）

**进度存储**：按单元独立存 localStorage（key = `vocab_done_<UnitId>`），换单元不丢，**设备间不互通**。

---

## 六、知识库

### 6.1 环境事实（实测）

| 项 | 状态 |
|---|---|
| Python 隔离环境 | `/Users/H.Sheng/.workbuddy/binaries/python/envs/default`（Pillow 12.3.0） |
| github.com | **不可达**（HTTPS 超时，且无 SSH 密钥） |
| gitee.com / netlify.com / vercel.com / surge.sh | 可达 |
| cloud.tencent.com / cos.ap-shanghai.myqcloud.com | 可达 |
| 沙箱内 pip 安装 | **受限** —— mkdir 被策略拦截（EEXIST，改 TMPDIR 无效） |

### 6.2 已踩过的坑

1. **zip 中文文件名编码** —— 包内中文名为 cp437 编码，需用 `filename.encode('cp437').decode('utf-8')` 还原，直接 `unzip` 会乱码。
2. **图片生成不能并行** —— 并行会写同名文件互相覆盖（曾 5 张只剩 2 张，白花积分）。必须一次一张，拿到立刻改名。
3. **图片生成 `output_dir` 参数无效** —— 图落默认目录，改名步骤不能省。
4. **原图 PNG 不能丢** —— 成品被覆盖后没有原图就重建不出来。
5. **WorkBuddy 内置发布不可靠** —— 见 6.3。
6. **预览服务会污染 HTML** —— 预览便携版时被注入 4 KB 的 `data-page-node-id` 属性。交付前必须校验 MD5。
7. **pip 在沙箱内装不上包** —— 改用标准库实现（COS 签名脚本即如此）。

### 6.3 决策记录：为什么放弃内置发布

| 时间 | 链接 | 实测结果 |
|---|---|---|
| 09:59 | `*.app.workbuddy.link` | 200（当时正常） |
| 10:57 | 同上 | **404「链接已失效」** |
| 10:59 | `*.e2b.sh3.sandbox.cloudstudio.club` | 200（当时正常） |
| 11:11 | 同上 | **403**；旧链接 **502** |

**结论**：该渠道在两个链接、两次发布、不到 20 分钟内全部失效，**不作为交付手段**。

### 6.4 决策记录：托管方案选择

**已排除**：
- GitHub Pages —— 本机连不上 github.com
- WorkBuddy 内置发布 —— 实测不可靠

**已选定**：
1. **便携版单文件**（首选，已交付）—— 零依赖、离线可用、永不失效
2. **腾讯云 COS**（进行中，待密钥）—— 国内访问快

**COS 已知风险**：腾讯云 COS 的默认域名常常不允许直接用浏览器打开网页（强制下载或 403），此时需绑定**已备案的自定义域名**。脚本内置自动探测，失败时提示改用**腾讯云 CloudBase 静态托管**（提供可直接访问、无需备案的 `*.tcloudbaseapp.com` 默认域名）。

### 6.5 部署命令备忘

**构建**：
```bash
cd /Users/H.Sheng/WorkBuddy/2026-09-09-09-42-17/mimi-vocab/007_米米背单词
/Users/H.Sheng/.workbuddy/binaries/python/envs/default/bin/python vocab-builder/build.py
```

**部署到 COS**（需先提供密钥）：
```bash
export COS_SECRET_ID=AKIDxxxxxxxx
export COS_SECRET_KEY=xxxxxxxx
export COS_REGION=ap-shanghai
export COS_BUCKET=<桶名>-<appid>
/Users/H.Sheng/.workbuddy/binaries/python/envs/default/bin/python vocab-builder/deploy_cos.py
```
脚本自动完成：建桶 → 设公有读 → 开静态网站 → 上传 → 探测公网 URL 是否真能打开。

**密钥获取**：https://console.cloud.tencent.com/cam/capi

---

## 七、验证记录汇总

| 验证项 | 方法 | 结果 |
|---|---|---|
| 资产完整性 | 文件计数 | 15 词 / 15 PNG / 15 JPG ✓ |
| 构建闭环 | 运行 build.py | `images ok`，无缺失图 ✓ |
| 数据一致性 | 重建产物 vs 沙箱产物 | **字节级一致（cmp 通过）** ✓ |
| 数据注入 | 解析 UNITS 常量 | 1 单元 / 15 词 / 15 图 ✓ |
| 占位符替换 | grep `__UNITDATA__` | 已替换，无残留 ✓ |
| 离线自包含 | 统计外部 JS/CSS | **0 个** ✓ |
| 四份成品一致 | MD5 | 全部 `4fc7287c...` ✓ |
| 便携版纯净度 | grep 注入属性 | 0 处残留 ✓ |
| COS 签名链路 | 假密钥干跑 | 403（请求正确到达，密钥无效为预期） ✓ |

---

## 八、待办与下一步

**进行中**
- [ ] 腾讯云 COS 部署 —— 待提供 SecretId / SecretKey / 桶名；若有已备案域名请一并告知

**后续可做**
- [ ] 新增单元 Text 2：提供词表 → 确认清单 → 报积分 → 生成插画 → 写入 JSON → 构建 → 交付
- [ ] 若 COS 默认域名受限，切换 CloudBase 静态托管

**给使用者的建议**
- 便携版发微信给米米，手机点开即用，建议在浏览器里「添加到主屏幕」，避免重复复制
- 进度存在各自浏览器本地，换设备需重新标记

---

## 九、备份说明

整个 `007_米米背单词/` 目录即全部资产，拷走即可继续工作（构建脚本路径自解析，搬到任何位置都能跑）。

**增量同步**（新增单元时只需搬）：
- `vocab-builder/units/TextN.json`
- `vocab-builder/images/TextN/`
- 重新生成的 HTML

**本记录文件**：`米米背单词-工作记录.md` / `.html`，随项目一同备份。

---

## 十、补充：GitHub Pages 上线与性能优化（2026-09-09 18:30）

### 10.1 Pages 正式上线

| 项 | 内容 |
|---|---|
| 地址 | https://dashopera.github.io/Ideha-vocab/ |
| 仓库 | `git@github.com:dashopera/Ideha-vocab.git`（main / root） |
| 开启方式 | Settings → Pages → Deploy from a branch → main / (root) |
| 托管方 | GitHub 官方，永久有效 |

### 10.2 性能问题与改造

**问题**：Pages 首次验证内容正确，但速度极差。631 KB 内嵌版三次实测耗时 **12.9s / 60s（超时，仅下载 125 KB）/ 32s**。

**量化定位**：成品 617 KB 中图片 base64 占 **580 KB（94%）**，HTML/JS 仅 37 KB。单个大文件在慢链上传输效率极低。

**方案：双版本输出**

| | 内嵌版 | 外链版 |
|---|---|---|
| 文件 | `背单词-学习卡片.html`、`米米背单词-便携版.html` | 根 `index.html`、`site/index.html` |
| 图片 | base64 内嵌 | 独立 `images/TextN/*.jpg` |
| 体积 | 631 KB（单文件） | 37 KB HTML + 435 KB 图片 |
| 用途 | 发微信/邮件、离线、永久可用 | GitHub Pages 在线访问 |

**关键约束**：GitHub Pages 从仓库根目录发布，Folder 只能选 `/ (root)` 或 `/docs`，不能指向 `site/`。因此外链版必须落在根 `index.html`。

**改造后实测**（连续三次）：0.8–1.0 秒首屏（首次部署冷启动 10.6s）。15 张图片全部 HTTP 200。

### 10.3 预览污染根治

预览服务会在打开 HTML 时注入 `data-page-node-id` 属性，本项目已因此三次交付/提交污染版本。本次从机制上解决：

1. **`vocab-builder/sanitize.py`** —— 正则剔除注入属性（本次一次清除 204 处）+ 内嵌版一致性自检，幂等可反复运行，`--check` 参数只校验不修改。
2. **`.git/hooks/pre-commit`** —— 提交时自动执行 sanitize，校验不通过则中止提交，净化结果自动 `git add`。

本次两次 commit 均由钩子自动净化，输出「✓ 内嵌版两份一致」。

### 10.4 过程中的一处自伤与修复

为支持外链版，给词数据加了 `_href` 字段，但忘记在内嵌版剔除，导致内嵌版 MD5 从 `f3586d5e` 漂移到 `a35b6012`（+631 字节）。

修复：`copy.deepcopy` 后 `w.pop("_href", None)`。内嵌版回归基准 `f3586d5e046d704d646bb912c3b114cf`（631,337 字节）。

### 10.5 更新后的标准流程

```bash
cd /Users/H.Sheng/WorkBuddy/2026-09-09-09-42-17/mimi-vocab/007_米米背单词
PY=/Users/H.Sheng/.workbuddy/binaries/python/envs/default/bin/python
$PY vocab-builder/build.py       # 双版本输出
$PY vocab-builder/sanitize.py     # 净化 + 自检
git add -A && git commit -m "说明" && git push
```

推送后 Pages 约 1 分钟完成重建。
